/* It creates a class called Profile. */
class Profile {
  constructor(status, message, body) {
    this.status = status;
    this.message = message;
    this.body = body;
  }
}
export default Profile;
